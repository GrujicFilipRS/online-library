# Online library

## How to start

Only prerequisite for startup is docker

You need to set up a `.env` file before starting. Check out `.env.example`

For dev version, run `docker compose -f docker-compose.dev.yml up`

For prod version, run `docker compose -f docker-compose.prod.yml up`